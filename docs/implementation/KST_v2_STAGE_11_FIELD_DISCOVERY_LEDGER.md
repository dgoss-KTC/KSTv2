# KST v2 -- Stage 11-A Long-Term Shortages Field Discovery Ledger

**Status:** Evidence-only discovery ledger. No field is an approved new implementation input until the corresponding result is confirmed in `KST_v2_STAGE_11_SOURCE_DISCOVERY_RESULTS.md` and owner review authorizes implementation planning.

| Projection or presentation fact | Candidate source and grain | Join / date / quantity meaning | Lifecycle and null behavior | Accepted rule supported or challenge | Discovery status |
|---|---|---|---|---|---|
| Report component population | Current MPS snapshot resolved parents, then current-effective multi-level `ps_mstr` occurrences | Parent is snapshot-scoped; BOM is domain + parent/component relationship effective on refresh date | Empty BOM is no component population; distinct components must not discard retained parent attribution | Scope selects report components only; it never allocates demand | Confirmed pattern |
| Workspace-parent attribution | Same BOM occurrences plus site-wide WO component events | Retain distinct resolved workspace parents per component; compare event WO parent to resolved scope | A component may have no workspace WO demand while remaining in population | `Q11-N03` found Taco component `115989` with one Taco parent, four other-program WO parents, and 62 site-wide component events. Calculate one balance only; retain attribution as detail. | Partially confirmed; eligible event population unresolved |
| Opening QOH (Stage 11-A specific) | Direct `ld_det` only | Domain + site + part aggregate: `COALESCE(SUM(ld_qty_oh), 0)`. Include Transit. Exclude `UPPER(ld_status)` MRB/INSPECT/NCMINSP and `UPPER(ld_lot)` prefixes RMA/RA. No positive-quantity, date/expiration, `loc_mstr`, `is_mstr`, `lad_det`, or `in_mstr` condition/join. | SQL null semantics are intentional: null `ld_status` or null `ld_lot` fails the respective `NOT IN` / `NOT LIKE` predicate and is excluded. No hard allocation is subtracted. Q11-T01 confirmed the source for `HDW-50M-00060`, `ICC-00994`, `ICC-01084`, and `ICC-01117`; the refreshed timeline snapshot reconfirmed `ICC-01117` at 37056 across 12 qualifying lots. | Settled owner-defined Stage 11-A source rule, 2026-09-15. | Confirmed |
| Hard allocation | `lad_det.lad_qty_all`, grain domain/site/WOID/operation/component/location/lot | `lad_nbr = wod_lot`; `lad_line = wod_op`; `lad_part = wod_part` | Positive allocation only; a matching `wod_det` row is not required to reserve shared physical inventory | Deduct only from owning WO remaining demand; never use `wod_qty_all` as extra coverage | Confirmed Stage 9 |
| WO demand identity | `wo_mstr` header + `wod_det` rows | Domain + `wo_lot = wod_lot`; component is `wod_part`; operation is `wod_op`; timing is header `wo_due_date` | The distinct demand identity is `(WOID, component, wod_op)`. Preserve all three keys in joins to prevent row multiplication; a component at multiple operations has separate explicit requirement rows. | Calculate row-level remaining demand, then sum all distinct component-operation rows into the component's weekly demand. | Owner decision confirmed, 2026-09-16 |
| WO demand quantity | `wod_qty_req`, `wod_qty_iss`, own usable `lad_qty_all` | `max(0, required - issued - own firm allocation)` at distinct WOID/component/operation grain | `wod_qty_pick` is not used in this QAD process and is excluded from Stage 11-A demand calculations. | Fully allocated/issued demand is zero | Owner decision confirmed, 2026-09-16 |
| WO eligibility | `wo_status`, current MPS P/e representation, `wo_bom_code` | Site/domain/header grain | Redesigned Q11-F02 confirms F and E as `wo_mstr.wo_status` values with detail rows. A/R/P were previously observed. There is no cancelled status; closed is `wo_status = 'C'`. `wo_bom_code = 'RMABOM'` is the complete QAD representation of RMA work for this report. | Include A/F/R/E/P; exclude closed and RMABOM | Owner decision confirmed, 2026-09-16 |
| Forecast demand | `mrp_det` dataset `fcs_sum` | Site/domain/component source row; bucket by `mrp_due_date`, QAD quantity without KST conversion | Q11-N06 returned no forecast rows for the timeline samples. Q11-F06 then selected a separate future candidate and confirmed nonmissing due date, positive quantity, and no duplicate `(mrp_nbr, mrp_line)` condition; its selected candidate had no `sod_det` MRP overlap. | Gross forecast remains interim and potentially overstated because forecast consumption and general source identity/overlap behavior remain unvalidated. | Partially confirmed; consumption/overlap unresolved |
| Conventional PO supply | `pod_det` + `po_mstr`, PO revision/line grain | Domain + PO number; component `pod_part`; due `pod_due_date`; quantity `pod_qty_ord - pod_qty_rcvd` | Q11-N04 observed C/X, past-due, unreceived, fully received, confirmed and unconfirmed shapes. Q11-F04 confirmed missing-due, partial-receipt, confirmed qualifying blank-status in-horizon, and unconfirmed qualifying blank-status in-horizon forms. Unexpected status was not observed. | Confirmed line-level supply candidate; only confirmed qualifying in-horizon lines can supply; other forms remain context. | Confirmed except unexpected-status sample |
| PO confirmation | `pod_det.pod__log01` | Nullable bit at PO line grain | Do not substitute `po_mstr.po_confirm` | Only confirmed line contributes supply | Confirmed mapping |
| PO display ordering | PO due date, PO number, PO line | `DueDate`, then PO number, then line | Null due date is exception context, not projected supply | Deterministic earliest qualifying in-horizon PO selection | Confirmed mapping |
| KSS | Effective scheduled `pod_det` + `po_mstr` relationship | Domain/site/component with pod and PO effective boundaries; `po_sched = 1` | Q11-N05 observed 651 active relationships and no active relationship for any timeline component. Q11-F05 confirmed scheduled relationships whose `po_eff_to` is expired while the PO-detail boundary remains current. | Classification only; no scheduled/KSS supply | Confirmed mapping and PO-effective expiry exclusion |
| Safety stock | `ptp_det.ptp_sfty_stk`, fallback `pt_mstr.pt_sfty_stk` | Selected-site component grain; selected-site row joined by domain/site/part, master by domain/part | Non-null site safety stock, including zero, is authoritative. Fall back to master only when selected-site row is absent. A present selected-site row with null safety stock is an unresolved source-data state: preserve it, do not treat it as zero, and do not fall back. Q11-F07 confirmed absent-site and zero-site forms; null is unobserved. | Owner decision 2026-09-15 governs severity threshold source/precedence. | Confirmed rule; null-state sample remains evidence gap |
| QAD status | `pt_mstr.pt_status` | Domain + component | Unknown code retained raw | Optional compact-list field | Needs Q11-07 |
| Lead time | `ptp_det.ptp_pur_lead`, master `pt_mstr.pt_pur_lead` fallback | Site-first days; display weeks are informational | Null/zero display behavior remains product decision | Informational only | Confirmed Stage 10 mapping |
| Planner / B/P | Site `ptp_buyer`, master `pt_buyer`, `code_mstr.code_user1` | Domain-scoped lookup plus source-field discriminator | Missing code/display stays blank; never cross-domain | Planner list field is readable display; B/P is code | Confirmed Stage 10 runtime mapping |
| Manufacturer item | `pod_det.pod_vpart` | PO line grain | Blank is blank | Component-level export uses selected earliest qualifying PO line | Confirmed mapping |
